import * as THREE from 'three';
import { VRMMaterial } from '../data/VRMMaterial';
export declare class VRMShaderMaterial extends THREE.ShaderMaterial {
    [key: string]: any;
    constructor(parameters?: THREE.ShaderMaterialParameters);
    fromMaterialProperty(property: VRMMaterial, textures: THREE.Texture[]): void;
}

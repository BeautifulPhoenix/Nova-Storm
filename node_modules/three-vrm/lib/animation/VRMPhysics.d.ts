import { VRM } from '../data';
export declare class VRMPhysics {
    private vrm;
    private springBoneGroups;
    private sphereColliderGroups;
    constructor(vrm: VRM);
    reset(): VRMPhysics;
    update(delta: number): VRMPhysics;
}

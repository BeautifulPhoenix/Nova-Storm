import * as THREE from 'three';
import { VRM } from '../data/VRM';
export declare class VRMLoader {
    private manager;
    private gltfLoader;
    constructor(manager?: THREE.LoadingManager);
    load(url: string, onLoad?: (vrm: VRM) => void, onProgress?: (request: ProgressEvent) => void, onError?: (event: ErrorEvent) => void): void;
    setCrossOrigin(crossOrigin: string): this;
    setPath(path: string): this;
    setDRACOLoader(dracoLoader: any): this;
}

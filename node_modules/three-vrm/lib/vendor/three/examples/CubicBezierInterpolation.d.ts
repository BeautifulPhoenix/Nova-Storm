import * as THREE from 'three';
export declare function createCreateInterpolant(parameterPositions: number[], sampleValues: number[], sampleSize: number, params: number[]): (result: number[]) => CubicBezierInterpolation;
export declare class CubicBezierInterpolation extends THREE.Interpolant {
    private interpolationParams;
    constructor(parameterPositions: number[], sampleValues: number[], sampleSize: number, resultBuffer: number[], params: Float32Array);
    interpolate_(i1: number, t0: number, t: number, t1: number): any;
    private _calculate;
}
